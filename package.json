{
  "name": "db-info",
  "description": "Node.js relational database information utility",
  "keywords": ["database", "db", "sqlite", "mysql", "PostgreSQL", "oracle", "db-oracle"],
  "version": "0.0.3",
  "engines": { "node" : ">=0.6.0" },
  "author": "Joe Ferner <joe.ferner@nearinfinity.com>",
  "maintainers": [
    { "name": "Joe Ferner", "email": "joe.ferner@nearinfinity.com" },
    { "name": "Jeff Kunkle", "email": "jeff.kunkle@nearinfinity.com" }
  ],
  "bugs": { "url": "https://github.com/nearinfinity/node-db-info/issues" },
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/nearinfinity/node-db-info.git" },
  "dependencies": {
  },
  "scripts": {
    "test": "istanbul cover _mocha -- -R spec --recursive ./tests && cat ./coverage/lcov.info | codecov",
    "localtest": "export GIT_BRANCH=`git rev-parse --abbrev-ref HEAD`; export GIT_COMMIT=`git rev-parse HEAD`; istanbul -x 'lib/drivers/sqlite3_parser.js' cover _mocha -- -R spec --recursive ./tests && cat ./coverage/lcov.info | codecov"
  },
  "devDependencies": {
    "mocha": "*",
	"istanbul": "*"
  }
}
